# Юзік — minimal requirements

# Web Frameworks
gradio>=5.25.2
fastapi
uvicorn
python-multipart

# AI SDKs
google-genai>=1.52.0
google-adk>=1.21.0

# Bot & Tools
python-telegram-bot[webhooks]
gradio_client==1.8.0
fal-client
requests
aiohttp>=3.12

# Helpers & Data
python-dotenv
rapidfuzz
datasets

# ── Text-to-Speech ──────────────────────────────────────────────────
# TTS_MODE=api  — only gradio_client (already above) is needed.
# TTS_MODE=local — uncomment the lines below (requires GPU + CUDA):
# git+https://github.com/tuteishygpt/coqui-ai-TTS.git
# huggingface_hub
